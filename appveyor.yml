before_build:
  - ps: gitversion /l console /output buildserver /updateassemblyinfo

build_script:
  - ps: dotnet pack -c Release src/TCode.r2rml4net -o ./nugets
  - ps: dotnet pack -c Release src/TCode.r2rml4net.CLI -o ./nugets

test_script:
  - dotnet test src/TCode.r2rml4net.Mapping.Tests
  - dotnet test src/TCode.r2rml4net.Tests

artifacts:
  - path: 'nugets/*.nupkg'
